package day_32_custommethods;

public class MethodsInJava {
    public static void main(String[] args) {
        //***************Methods in Java*****************
        /*There are 2 main parts of a Class in Java.
        1.data/properties/information
        2.behaviour/function/actions/METHODS-->
        we are making the code in the method REUSABLE.
        So far we have been using several methods from different classes.
    for example:
        println(print something)
        nextInt()from scanners class
    String class methods:
        length()
        equals()
        substring
     Array class
        toString()
        sort()
        deepToString()
        DRY-->Don't repeat yourself. Create a method
        Methods can be of 4 types:
        1. Method that does noy accept Input and does not return value
           System.out.println();-->only return does not accept value
        2. Method that accept a value and does not return any value
           Arrays.sort(array);
           System.out.println("I love Java")
        3. Method that does not accept input and returns a value
           str.length();
           str.toUpperCase("java")
           st.trim();
        4.Method that accepts input and return a value
        String word="Java"
        str.chatAt(0);--->it is gonna return J
   .....................................................................................
   First custom method

   public static void displayMessage(){
   System.out.println("Hello World");
   }
   public----> access modifier.public means this can be used anywhere in your project.Visible to Word
   static---> can be used without creating object
   void---> it does not return any value
   displayMessage--->name of the method.camelCase, as meaningful as possible
   By writing custom methods, we are now able to make reusable codes.





         */

    }
}
